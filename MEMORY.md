# MEMORY.md

Curated long-term memory (high-signal, durable only).

## Current focus
- Building PocketPivot course sales flow with Stripe + Cloudflare Worker.

## Ops notes
- Detailed daily logs remain in `memory/YYYY-MM-DD.md`.
- Tiered references now live in `memory/hot`, `memory/warm`, and `memory/cold`.
- Retrieval/flush/compaction policy is defined in `memory/ops-config.json`.
